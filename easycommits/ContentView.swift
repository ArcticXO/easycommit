//
//  ContentView.swift
//  easycommits
//
//  Created by Ali Al Abdullah on 02/02/2024.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("so when i change this it should change online, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
